// Interval Primitives Test Support.swift
//
// Test Support spine per [MOD-024]. No fixtures yet; the shell re-exports
// the package's own product so the namespace stays uniform. The package's
// only product dep (Pair Primitives) publishes no Test Support product, so
// there is no upstream spine link to anchor on.

@_exported public import Interval_Primitives
